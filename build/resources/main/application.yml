# mysql database settings
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ssu_blog?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: park2057
    jpa:
      hibernate.ddl-auto: update
      database: mysql
      repositories:
        enabled: true